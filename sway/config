xwayland force
exec "$scriptd/export-envs.sh"

set $rootd $HOME/.config/sway
set $confd $rootd/modules.d
set $scriptd $rootd/scripts

set $term foot
set $menu wofi -s $confd/wofi.css

include init
include ./sway.d/*

