xwayland force
#exec "dbus-update-activation-environment --systemd --all"
exec systemctl --user import-environment GTK_USE_PORTAL
exec hash dbus-update-activation-environment 2>/dev/null && \
     dbus-update-activation-environment --systemd GTK_USE_PORTAL

set $rootd $HOME/.config/sway
set $confd $rootd/modules.d
set $scriptd $rootd/scripts

#set $term flatpak run org.wezfurlong.wezterm
set $term alacritty
set $menu wofi

include ./sway.d/*.conf

